<%- model_class = PayrollType -%>
<div class="page-heading">
  <h1><%=t '.title', :default => model_class.model_name.human(:count => 2) %></h1>
  <div class="options">
    <div class="btn-toolbar">
      <% if can? :create, PayrollType %>
        <%= link_to new_payroll_type_path, :class => 'btn btn-primary' do %>
          <i class="fa fa-plus"></i>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<div class="container-fluid">
  
  <%= render "/layouts/message" %>

  <div class="row">
    <div class="col-xs-12">
      <div class="panel panel-primary">
        
        <div class="panel-heading"></div>

        <div class="panel-body panel-no-padding">
          <table class="table table-striped">
            <thead>
              <tr>
                <th><%= model_class.human_attribute_name(:description) %></th>
                <th><%= model_class.human_attribute_name(:payroll_type) %></th>
                <th><%=t '.actions', :default => t("helpers.actions") %></th>
              </tr>
            </thead>
            <tbody>
              <% @payroll_types.each do |payroll_type| %>
                <tr>
                  <td><%= payroll_type.description %></td>
                  <td><%= t("payroll_type.#{payroll_type.payroll_type}") %></td>
                  <td>
                    <% if can? :update, PayrollType %>
                      <%= link_to edit_payroll_type_path(payroll_type), :class => 'btn btn-xs btn-success-alt' do %>
                        <i class="fa fa-pencil"></i>
                      <% end %>
                    <% end %>

                    <% if can? :destroy, PayrollType %>
                      <%= link_to payroll_type_path(payroll_type), :method => :delete,
                                  :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                                  :class => 'btn btn-xs btn-danger-alt' do %>
                        <i class="fa fa-trash-o"></i>
                      <% end %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
