<% @products.each do |product| %>
<tr>
  <td><%= check_box_tag product.id %></td>
  <td><%= link_to product.code, product_path(product) %></td>
  <td><%= product.name %></td>                                       
</tr>
<% end %>