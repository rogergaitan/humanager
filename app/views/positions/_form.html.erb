<div class="container-fluid">
  <div data-widget-group="group1" class="ui-sortable">

    <% if @position.errors.any? %>
      <div class="alert alert-dismissable alert-danger">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <h3><%= pluralize(@position.errors.count, "error") %> no permitieron crear el puesto:</h3>
        <% @position.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
        <br>
      </div>
    <% end %>

    <div class="panel panel-primary" data-widget="{&quot;draggable&quot;: &quot;false&quot;}" data-widget-static="">

      <div class="panel-heading"></div>

      <div class="panel-body panel-no-padding">
        <%= form_for @position, :html => { :class => 'form-horizontal row-border',
          :data => {:parsley_validate => ""} } do |f| %>
          
          <div class="form-group">
            <%= f.label :position, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-3">
              <%= f.text_field :position, :class => 'form-control', :required => true %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :description, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-3">
              <%= f.text_area :description, :class => 'form-control' %>
            </div>
          </div>

          <div class="panel-footer">
            <div class="row">
              <div class="col-sm-8 col-sm-offset-2">
                <%= f.submit :class => 'btn btn-primary' %>
                
                <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                              positions_path, :class => 'btn' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
