<%- model_class = Taxis -%>
<div id="section-body" class="tabbable">
  <ul class="nav nav-tabs">
    <li class="active">
      <a data-toggle="tab" href="#tab1">
        <%=t '.title', :default => model_class.model_name.human.pluralize %>
      </a>
    </li>
  </ul>
  <div class="tab-content">
    <div class="row-fluid">
      <div id="accordion1" class="accordion">
        <div class="accordion-group">
          <div class="accordion-heading">
            <a class="accordion-toggle" data-original-title="" href="#show" data-toggle="collapse">
              <i class="icon-list-alt icon-white"></i>
              <span class="divider-vertical"></span>
              <%=t '.title', :default => model_class.model_name.human %>
              <i class="icon-chevron-down icon-white pull-right"></i>
            </a>
          </div>
          <div id="show" class="accordion-body in collapse" style="height: auto;">
            <div class="accordion-inner paddind">
              <dl class="dl-horizontal">
                <dt><strong><%= model_class.human_attribute_name(:name) %>:</strong></dt>
                <dd><%= @taxis.name %></dd>
                <dt><strong><%= model_class.human_attribute_name(:percentage) %>:</strong></dt>
                <dd><%= @taxis.percentage %></dd>
                <dt><strong><%= model_class.human_attribute_name(:cc_id) %>:</strong></dt>
                <dd><%= @taxis.cc_id %></dd>
              </dl>
              <div class="form-actions">
                <%= link_to t('.back', :default => t("helpers.links.back")), taxes_path, :class => 'btn btn-maniadmin-8' %>
                <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_taxis_path(@taxis), :class => 'btn btn-maniadmin-2' %>
                <%= link_to t('.destroy', :default => t("helpers.links.destroy")), taxis_path(@taxis), :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                            :class => 'btn btn-maniadmin-5' %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>