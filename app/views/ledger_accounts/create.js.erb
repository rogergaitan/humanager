$("#messages").html('<div class="flash notice"><%= escape_javascript(flash.discard(:notice)) %></div>');
$("input#<%= @ledger_account.ifather %>")
	.closest("tr")
	.after("<%= escape_javascript(render(:partial => 'ledger_account')) %>");